//import{sum,shopping_cart,login,fetchData} from './sum'

describe('Calculator operations',()=>{
    test ('adds two numbers correctly',()=>{
        expect(1+2).toBe(3);
    });
    it('sub',()=>{
        expect(5-3).toBe(2);
    });
});